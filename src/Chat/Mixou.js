// Components
import { Typography } from '@mui/material';
import Theme from './Theme';

const Mixou = () => {
  return (
    <Theme>
      <Typography> Welcome on Mixou Chat!</Typography>
    </Theme>
  );
};

export default Mixou;